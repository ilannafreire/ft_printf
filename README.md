# ft_printf
ft_printf — A C library reimplementing the standard printf() function, handling conversions for %c, %s, %p, %d, %i, %u, %x, %X, and %% using variadic functions. Built as part of the 42 curriculum.
